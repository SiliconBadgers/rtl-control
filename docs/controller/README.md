# docs/controller

Controller diagram, interfaces, state/control flow and command walkthroughs for rtl-control#2. Keep editable source, plus SVG/PNG preview for draw.io. Link the architecture register proposal instead of copying the maps.

See [the current assignment](../../docs/START-HERE.md).
